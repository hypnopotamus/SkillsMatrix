{
  "name": "skills-matrix",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^7.4.0"
  },
  "scripts": {
    "start": "concurrently \"cd UI && npm start\" \"cd server && npm start\"",
    "build": "concurrently \"cd UI && npm run build\" \"cd server && npm run build\"",
    "test": "concurrently \"cd UI && npm test\" \"cd server && npm test\""
  }
}
