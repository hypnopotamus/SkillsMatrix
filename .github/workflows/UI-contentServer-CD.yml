on:
  workflow_run:
    branches:
      - master
    workflows: 
      - "Continuous Integration"
    types:
      - completed
    paths:
      - ./ContentServer/**

jobs:
  cd:
    uses: ./.github/workflows/CD.yml
    with:
      path: ./ContentServer