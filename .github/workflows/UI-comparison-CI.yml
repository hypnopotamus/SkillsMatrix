on:
  push:
    branches: [ $default-branch ]
    paths:
      - ./UI/Comparison/**
  pull_request_target:
    branches: [ $default-branch ]
    paths: 
      - ./UI/Comparison/**

jobs:
  cd:
    uses: ./.github/workflows/CI.yml
    with:
      path: ./UI/Comparison
    